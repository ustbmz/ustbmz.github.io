import{_ as e,o as s,c as t,U as a}from"./chunks/framework.76b79cb5.js";const i="/assets/5e9d3fdd0001a52315540894.06ac184e.png",o="/assets/5e9d3fe60001932e20560946.324fe671.png",p="/assets/5e9d3fef0001594212380190.9b4becc8.png",n="/assets/5e9d3ff60001362113640292.4e462986.png",c="/assets/5e9d3ffc0001076113920496.ec8ad715.png",d="/assets/5e9d4003000121fe20360746.d53fc74d.png",r="/assets/5e9d40090001d7de13920598.ed88842d.png",l="/assets/5e9d400f0001c06d13920462.e816ceca.png",g="/assets/5e9d40150001b65d14480530.bc2c42a5.png",u="/assets/5e9d401b00018a6514480224.4731f0a4.png",_="/assets/5e9d4021000149c215600258.59eaccf7.png",h="/assets/5e9d4026000196c915600836.35b12fd3.png",P=JSON.parse('{"title":"15 配置多个远端仓库：把代码推送到不同的服务器","description":"","frontmatter":{},"headers":[],"relativePath":"frontend/git/15 配置多个远端仓库：把代码推送到不同的服务器.md","filePath":"frontend/git/15 配置多个远端仓库：把代码推送到不同的服务器.md"}'),m={name:"frontend/git/15 配置多个远端仓库：把代码推送到不同的服务器.md"},b=a('<h1 id="_15-配置多个远端仓库-把代码推送到不同的服务器" tabindex="-1">15 配置多个远端仓库：把代码推送到不同的服务器 <a class="header-anchor" href="#_15-配置多个远端仓库-把代码推送到不同的服务器" aria-label="Permalink to &quot;15 配置多个远端仓库：把代码推送到不同的服务器&quot;">​</a></h1><p>有时候，我们需要将一个改动推送到多个远程仓库去，比如说你做了一个开源项目，既想发布到 GitHub 又想发布到国内的码云上，如果建立两个文件夹分别去推送，就非常麻烦，其实 Git 已经有相关的功能，在这一节中我们来学习如何使用。</p><h2 id="_15-1-环境准备" tabindex="-1">15.1 环境准备 <a class="header-anchor" href="#_15-1-环境准备" aria-label="Permalink to &quot;15.1 环境准备&quot;">​</a></h2><p>首先我去 GitHub 上创建一个远程仓库，创建地址为 <code>https://github.com/new</code> ，如下图所示：</p><p><img src="'+i+'" alt="图片描述"></p><p>在表单下方有一个提交表单按钮，点击后会自动跳转到仓库页面，如下图所示：</p><p><img src="'+o+'" alt="图片描述"></p><p>在上图中可以看到有一个仓库地址，我们把仓库地址复制一下，然后打开终端窗口，然后在终端窗口中查看当前的地址，参考命令如下图所示：</p><div class="language- line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">git remote -v</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>命令执行后，返回信息如下图所示： <img src="'+p+'" alt="图片描述"> 在上图中可以看到远程仓库地址信息，我们在这些信息中可以看到两个 origin，这两个 origin 是代表远程仓库的一个名字，默认情况下就是 origin；</p><p>在地址后面有一个括号里面有 <code>push</code> 和 <code>fetch</code> 两种，当我们执行 <code>git push</code> 时候他会调用对应 <code>push</code> 地址，同样当我们执行命令 <code>git featch</code> 的时候则会调用 <code>fetch</code> 地址；当拉取代码之后默认情况下会产生一个远程仓库 origin，并且对应的 push 地址也只有一个；</p><h2 id="_15-2-添加多个仓库" tabindex="-1">15.2 添加多个仓库 <a class="header-anchor" href="#_15-2-添加多个仓库" aria-label="Permalink to &quot;15.2 添加多个仓库&quot;">​</a></h2><p>在前面的内容中，我们学习了使用命令 <code>git push</code> 把代码推送到远程仓库中去，其实这条命令是一条简写的命令，因为我们没有指定仓库地址，完整的命令是 <code>git push origin</code>，在执行简写命令时 Git 会找一个默认的仓库；如果我们想把代码往多个代码托管平台推送只需要添加一个远程地址即可，参考命令如下：</p><div class="language- line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">git remote add github https://github.com/78778443/gittest.git</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>命令参数 <code>git remote add</code> 是添加远程仓库地址固定写法，<code>github</code> 是我给远程仓库自定义的一个名字，<code>https://github.com/78778443/gittest.git</code> 则是的远程仓库地址，命令执行完成之后，我们再使用 <code>git remote -v</code> 查看远程地址列表，返回信息如下图所示：</p><p><img src="'+n+'" alt="图片描述"> 在图中可以看到已经新增了一个 <code>github</code> 的仓库地址，接下来，我们使用完整的推送命令把代码推送到 GitHub 的代码仓库当中去，参考推送命令如下：</p><div class="language- line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">git push github</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>命令执行后，返回的信息如下图所示：</p><p><img src="'+c+'" alt="图片描述"> 在图中可以看到，GitHub 需要让我进行认证，输入用户名和密码之后，成功把本地代码推送到了远程仓库中；我们回到 GitHub 网页中，可以看到已经上传上来，如下图所示：</p><p><img src="'+d+'" alt="图片描述"> 在上图中可以看到 GitHub 页面当中已经可以看到我们刚才推送上来的代码。</p><p>接下来我们再尝试用简写的推送命令推送代码，看看会把修改推送到哪一个仓库中去，执行的命令和返回结果如下图所示：</p><p><img src="'+r+'" alt="图片描述"> 在图中可以看到，当我使用简写的推送命令进行推送时候，并不会往 GitHub 这个仓库进行推送，只会往默认的 origin 仓库中推送。</p><p>在这里说一个小技巧，如果我们想将通过简写的命令让他推送到 GitHub 而不是 origin，可以在推送的命令中加上 <code>-u</code> 参数来进行修改默指定的仓库，参考命令如下：</p><div class="language- line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">git push -u github</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这句命令执行之后，会首先将代码推送到 GitHub 仓库中，并且以后使用 <code>git push</code> 推送时也会往 GitHub 中推送，执行结果如下图所示：</p><p><img src="'+l+'" alt="图片描述"> 在返回的信息中可以看到已经把当前的内容推送到 Git 仓库中，并且成功修改了推送默认的仓库。</p><p>接下来我们来验证一下刚才的操作正确性，我们随意修改一个文件里面的内容，然后提交到本地仓库，最后使用简写的推送命令 <code>git push</code> 进行推送，执行的命令和返回结果如下图所示：</p><p><img src="'+g+'" alt="图片描述"></p><p>在图中可以看到当执行了 <code>git push</code> 之后，已经变成了 GitHub 的地址，说明修改默认的推送地址成功。</p><h3 id="_15-3-一个仓库多个地址" tabindex="-1">15.3 一个仓库多个地址 <a class="header-anchor" href="#_15-3-一个仓库多个地址" aria-label="Permalink to &quot;15.3 一个仓库多个地址&quot;">​</a></h3><p>有时候你可能还有这种需求，就是想推送的时候执行一条命令，就把代码往两个地址同时推送，这种操作 Git 也是支持的；和上一小节往 Git 中配置两个远程仓库的方式略有不同，我们可以在一个远程仓库中配置多个 push 地址，下面我们来实践一下。</p><p>首先我们把上面一小节中添加的 GitHub 地址给删除，让仓库恢复成之前的效果，删除 GitHub 远程仓库执行的命令和删除验证的结果如下图所示： <img src="'+u+'" alt="图片描述"> 在上图中可以看到已经删除成功， 使用 <code>git remote -v</code> 展示的只保留了 origin 的地址。</p><p>接着我们往 origin 仓库中添加多个地址，参考的命令如下：</p><div class="language- line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki material-theme-palenight"><code><span class="line"><span style="color:#A6ACCD;">git remote set-url --add origin https://github.com/78778443/gittest.git</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>命令执行之后，我们再次使用 <code>git remote -v</code> 来查看远程仓库地址信息，返回结果如下图所示：</p><p><img src="'+_+'" alt="图片描述"></p><p>在返回的信息中，可以看到在 <code>origin</code> 远程仓库中增加了一个 GitHub 的 <code>push</code> 地址，这个时候我们再去修改内容，然后推送查看效果，如下图所示： <img src="'+h+'" alt="图片描述"> 在上图中可以看到，只需要执行 <code>git push</code> 命令，Git 就会一次性把本地仓库的代码推送到多个远程地址去。</p><h2 id="_15-4-小结" tabindex="-1">15.4 小结 <a class="header-anchor" href="#_15-4-小结" aria-label="Permalink to &quot;15.4 小结&quot;">​</a></h2><p>在这一节中我们主要学习了 Git 远程仓库的管理，解决如何把一份代码快速推送到多个仓库中，主要有以下两种方式：</p><ol><li>新增多个远程仓库，不同的仓库配置不同的仓库地址</li><li>新增多个远程仓库地址，把多个远程仓库地址放到一个仓库中</li></ol>',40),f=[b];function v(C,G,y,H,A,k){return s(),t("div",null,f)}const x=e(m,[["render",v]]);export{P as __pageData,x as default};
